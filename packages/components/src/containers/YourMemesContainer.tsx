import React from 'react';
import { SafeAreaView } from 'react-native';
import { Title } from 'react-native-paper';

export default function YourMemesContainer() {
    return (
        <SafeAreaView>
            <Title style={{ alignSelf: 'center' }}>
                Your memes
            </Title>
        </SafeAreaView>
    )
}
